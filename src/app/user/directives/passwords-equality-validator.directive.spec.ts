import { PasswordsEqualityValidatorDirective } from './passwords-equality-validator.directive';

describe('PasswordsEqualityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsEqualityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
